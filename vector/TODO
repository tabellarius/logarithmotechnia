+ SetOptions() + config() (сбор опций с Payload и Vector для лёгкого создания векторов) + добавить в функции, возвращающие Payload
Remove report
Appliers for different return types
Factors
Helpers for payloads
Rework Config
(?) Make all payload public
(?) Convertor (transform values into []interface{})
String functions
Comparator
Default NA Values
R-style reuse of vector and boolean slices
Vector's element indices as uint32
