Adjust (expand or contract a vector to the given size)
Appliers for different return types
Factors
Remove report
(?) Make all payload public
String functions
Comparator
Finder interface for InterfacePayload
Default NA Values
R-style reuse of vector and boolean slices
Vector's element indices as uint32
Add df.Err and logger
Update() + Clone()
(?) Remove SupportsWhicher() and SupportsFilter()
Options() + unified *Payload()
Convertor() - install convertors to arbitrary payloads
Change Append(Vector) to Append(Payload) for payloads